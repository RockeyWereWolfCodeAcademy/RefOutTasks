# C# "Value and Reference types | Ref out" Named task

## Description:

Write the method that converts the negative elements of the array of numbers into positive ones. (array cannot be sent without assign)
